apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataverseup.fullname" . }}
  labels:
    {{- include "dataverseup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataverseup.primarySelectorLabels" . | nindent 4 }}
