{{- if and .Values.persistence.enabled (empty (.Values.persistence.existingClaim | default "")) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "dataverseup.fullname" . }}-data
  labels:
    {{- include "dataverseup.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.size | quote }}
  {{- with .Values.persistence.storageClassName }}
  storageClassName: {{ . }}
  {{- end }}
{{- end }}
