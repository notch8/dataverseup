# Dev only: configbaker overwrites this file on first successful `dev_bootstrap` with lines like:
#   API_TOKEN=<uuid>
# `dev_branding` and `dev_seed` refresh secrets/api/key from this API_TOKEN whenever it is set (after bootstrap).
